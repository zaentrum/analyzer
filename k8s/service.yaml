apiVersion: v1
kind: Service
metadata:
  name: analyzer
  namespace: stube
  labels:
    app: analyzer
spec:
  type: ClusterIP
  selector:
    app: analyzer
  ports:
    - name: http
      port: 80
      targetPort: 8080
